/*
 * função recursiva - função que chama a si própria
 * 
 * 
 * interface - possui métodos abstratos (para a validação) 
* não há herança, mas sim um implementação da interface para classe *
Os métodos da interface são abstratos, então a gente pode reescrevê-los nas classes onde se está utilizando a interface

extends -> herança
implements -> interface

utilizamos interface quando temos vários tipos de classes diferentes (o que nos impossibilita da utilização de herança)

a interface não possui uma ligação direta, mas sim um "contrato" com as classes
 * 
 * 
 * 
*/
